services:
  - type: web
    name: extractor-facturas-exogena
    runtime: python
    plan: free
    buildCommand: pip install -r requirements_web.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
